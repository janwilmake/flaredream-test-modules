demonstration of different filetypes that can all be imported into flaredream.

as of now they all work by default, but `wasm` doesn't work yet as it's a binary and im not adding it to the bundle atm
